alembic==1.19.1
asyncpg==0.31.0
bcrypt==5.0.0
email-validator==2.3.0
fastapi==0.141.1
httpx==0.28.1
psycopg==3.3.4
psycopg-binary==3.3.4
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
pyotp==2.10.0
pytest==9.1.1
pytest-asyncio==1.4.0
# NOTA (auditoría de dependencias, sep-2026): python-jose declara `ecdsa`
# como dependencia DURA (no opcional/extra) sin importar qué extra se
# instale — probado: `python-jose[cryptography]` sigue arrastrando
# `ecdsa`. PYSEC-2026-1325 (timing attack Minerva sobre P-256) queda
# como riesgo aceptado y documentado en STATE.md: esta app firma
# exclusivamente con HS256 (app/core/security.py), así que la superficie
# ECDSA de `ecdsa` nunca se ejerce en runtime. Sin parche disponible
# upstream a la fecha.
python-jose==3.5.0
# 0.0.20 tenía 6 CVEs conocidos (varios DoS reales de parsing +
# PYSEC-2026-3037, contrabando de parámetros HTTP vía `;` como separador
# que sí aplica a `request.form()` de Starlette/FastAPI) — mismo hallazgo.
python-multipart==0.0.32
SQLAlchemy==2.0.52
uvicorn==0.52.4
# Agregados en el cierre del módulo 24 (reports) — Exportación de Datos
# [core] a XLSX/PDF. NO instalados/verificados en el entorno donde se
# escribió este módulo (sin red — ver STATE.md, sección `reports`).
openpyxl==3.1.5
reportlab==4.2.5
